<h1><%= @task.title.upcase  %></h1>

<p><%= @task.details %></p>

<p><%= @task.completed ? "This task is completed" : "This task is not yet completed" %></p>

<%= link_to "Back to list", tasks_path %>

<%= link_to "Edit a task", edit_task_path %>
